<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:App1.ViewModels;assembly=App1"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:DataType="viewModels:LoginViewModel"
             x:Class="App1.MainPage">
    <ContentPage.BindingContext>
        <viewModels:LoginViewModel></viewModels:LoginViewModel>
    </ContentPage.BindingContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackLayout Grid.Row="0">
        <!-- Place new controls here -->
        <Label Text="Welcome to Xamarin.Forms!" 
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand"  />
        <Entry  Text="{Binding Username}" TextColor="Black"></Entry>
        <Entry Text="{Binding Password}" TextColor="Black"></Entry>
        <Button Text="Submit" Command="{Binding Login}">
          
        </Button>
        </StackLayout>
        <StackLayout Grid.Row="1" Padding="10,0,10,0">
      
        
    </StackLayout>
    </Grid>

</ContentPage>
